spring:
  application:
    name: AuthService
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DATABASE:vet_clinic_db}
    username: root
    password: root
  jpa:
    show-sql: true



server:
  port: 8081