spring:
  application:
    name: visitbackend
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DATABASE:pet_clinic}
    username: root
    password: root
  jpa:
    show-sql: true



server:
  port: 9003